{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bambuchastudent.github.io/winampmusic/ampula/schema/ampula-1.schema.json",
  "title": "Ámpula Core v1",
  "description": "A small portable memory of a musical moment.",
  "type": "object",
  "required": ["format", "version", "tracks"],
  "additionalProperties": true,
  "properties": {
    "format": { "const": "ampula" },
    "version": { "const": "1" },
    "capturedAt": { "type": "string", "format": "date-time" },
    "startTrack": { "type": "integer", "minimum": 0 },
    "moment": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "title": { "type": "string", "maxLength": 240 },
        "note": { "type": "string", "maxLength": 4000 }
      }
    },
    "tracks": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/track" }
    }
  },
  "$defs": {
    "track": {
      "type": "object",
      "required": ["title", "artists"],
      "additionalProperties": true,
      "properties": {
        "title": { "type": "string", "minLength": 1 },
        "artists": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "album": { "type": "string" },
        "versionLabel": { "type": "string" },
        "durationMs": { "type": "integer", "minimum": 0 },
        "isrc": { "type": "string" },
        "musicBrainzRecordingId": { "type": "string" },
        "fingerprint": {
          "type": "object",
          "required": ["algorithm", "value"],
          "additionalProperties": true,
          "properties": {
            "algorithm": { "type": "string", "minLength": 1 },
            "value": { "type": "string", "minLength": 1 }
          }
        },
        "cue": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "startMs": { "type": "integer", "minimum": 0 },
            "endMs": { "type": "integer", "minimum": 0 }
          }
        },
        "observations": {
          "type": "array",
          "items": { "$ref": "#/$defs/observation" }
        }
      }
    },
    "observation": {
      "type": "object",
      "required": ["service"],
      "additionalProperties": true,
      "properties": {
        "service": { "type": "string", "minLength": 1 },
        "itemId": { "type": "string" },
        "url": { "type": "string", "format": "uri" },
        "observedAt": { "type": "string", "format": "date-time" },
        "representation": { "type": "string" }
      },
      "anyOf": [
        { "required": ["itemId"] },
        { "required": ["url"] }
      ]
    }
  }
}
