name: Apple playlist hit probe

on:
  pull_request:
    branches: [develop]
    paths:
      - 'tests/apple-playlist-hit-probe.mjs'
      - '.github/workflows/apple-playlist-hit-probe.yml'

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tests/apple-playlist-hit-probe.mjs
