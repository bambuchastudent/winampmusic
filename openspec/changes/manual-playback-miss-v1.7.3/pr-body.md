# PR body draft

Adds generic manual playback rejection (`MISS`) without mutating canonical identity. Rejected YouTube IDs persist locally, are excluded before candidate ranking, and replacement playback remains subject to final trust. Wrong audio is suspended immediately; exhausted recovery advances to later playable rows.
