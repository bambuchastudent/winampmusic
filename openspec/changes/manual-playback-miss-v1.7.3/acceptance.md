# Acceptance

Production evidence: a canonical Spotify row can report `NO TRUSTWORTHY YOUTUBE MATCH · ORIGIN PRESERVED` while the hidden YouTube iframe is still actively playing the rejected stored candidate. v1.7.3 treats that playback candidate as mutable cache and gives the user an explicit local rejection action without altering canonical identity.
