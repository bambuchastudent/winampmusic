# Tasks

- [x] Inspect current `develop`, AGENTS.md, README.md, index.html, Spotify importer tests and existing Spotify fallback OpenSpec.
- [x] Add regression contract for a 160-track playlist truncated to 100 by the primary adapter.
- [ ] Implement primary truncation detection and paginated completion.
- [ ] Preserve partial primary metadata if completion fails.
- [ ] Run targeted and existing Spotify import contracts in CI.
- [ ] Merge to `develop` and verify Pages + release.