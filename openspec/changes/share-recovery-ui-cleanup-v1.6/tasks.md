# Tasks

- [x] Add behavioral coverage for reopening a historical `?p=` link with empty local storage.
- [x] Update action/UI tests for `Share`, no primary `.ampula` open button, and compact library heading.
- [x] Add a lazy receive-only legacy share compatibility adapter.
- [x] Route initial `p`/`s` URLs to the compatibility adapter without changing canonical `?a=` receive.
- [x] Make Clear remove share transports from the current URL before reload.
- [x] Simplify library header, Share dialog, and QR copy so transport/format jargon is not primary UI.
- [x] Update README sharing flow and compatibility notes.
- [x] Run the behavioral suite and fix regressions before merge.
