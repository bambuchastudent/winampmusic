# Tasks

- [ ] Add behavioral coverage for reopening a historical `?p=` link with empty local storage.
- [ ] Update action/UI tests for `Share`, no primary `.ampula` open button, and compact library heading.
- [ ] Add a lazy receive-only legacy share compatibility adapter.
- [ ] Route initial `p`/`s` URLs to the compatibility adapter without changing canonical `?a=` receive.
- [ ] Make Clear remove share transports from the current URL before reload.
- [ ] Simplify library header, Share dialog, and QR copy so transport/format jargon is not primary UI.
- [ ] Update README sharing flow and compatibility notes.
- [ ] Run the behavioral suite and fix regressions before merge.
